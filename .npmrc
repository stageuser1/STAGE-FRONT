# The `engines` range in package.json is a statement about what this app is
# built and verified against (Node 22 LTS), not a hint. Without engine-strict
# npm only warns, and a runner on a different major would install and build
# anyway — which is exactly the "no runtime version declaration" gap the
# pre-launch audit recorded (P2-6).
engine-strict=true
