# STAGE FRONT · 固定工作约定

本文件在每个 session 开始时自动载入,所以这里写的是**必须执行的动作**,
不是"最好记得做"的提醒。

## 提交后立即 push

**每次 `git commit` 之后,立刻 `git push`。不要攒着。**

理由不是整洁,是正确性:Vercel 从 `origin/main` 构建。未推送的提交意味着
**生产上跑的是旧代码**,于是:

- 任何"生产实测"验证的都是旧代码,结论无效;
- 环境变量、依赖、路由的改动在生产不存在,排查会指向完全错误的方向。

2026-08-09 因此发生过两次故障排查:一次是清理 `DIRECTUS_*` 变量后构建失败
(生产还在跑迁移前的代码),一次是阶段二 `/api/schools` 在生产返回 404
(路由根本没部署)。两次都浪费了往复,第二次是在明知第一次教训的情况下重犯的
—— 所以它从"记得做"升级成本文件里的固定动作。

例外:确实不打算发布的实验性提交。这种情况要**明确说出来**,而不是默默不推。

## 复核判据顺序

线上 curl 输出 > 线上日志 > 本地复现 > 源码推断。

从源码推断出的结论,写进报告前必须先上线验一次(阶段一有过一次因此产生的
阻塞级误判,见 `docs/roadmap/OSS_MIGRATION_PROMPTS.md` 总则)。

## 验证"状态翻转"类行为时,先确认起点

测"撤回后不可见"之前,必须先确认它**真的可见过**。在一个本来就是 404 的
页面上测"撤回后变 404",测不出任何东西 —— 阶段二的阻塞级验收项差点因此
假绿(实际情况是 `revalidatePath` 对具体路径无效,页面撤回后仍返回 200)。

## 迁移与架构决策

见 `docs/roadmap/OSS_MIGRATION_PROMPTS.md`(分阶段提示词 + 第 6 节裁决记录)
与 `docs/roadmap/OSS_PHASE1_DELIVERY.md`。
