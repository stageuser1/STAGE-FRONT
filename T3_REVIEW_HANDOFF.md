# T3 移交文档 —— Web Card + 专业详情页

**状态:已结项(已通过评审)。** Codex 规则层评审四轮通过(2026-08-03),
其后一次跨 ticket 缺陷(T3-R6)由真实数据触发、经 T3-R7 完成对修复本身的
复核,**均已结项,不再开新轮次**。

| | |
|---|---|
| Ticket | T3(蓝图 §2.1 Web Card + §2.2 详情页) |
| 仓库 | `stagefront`(`D:\STAGE FRONT`) |
| 规格 | `STAGE_V3_交接蓝图.md` §0.4 / §1.5 / §2.1 / §2.2 / §3 全文 |
| 数据契约 | 数据字典 §5–7 / §13(`stage-music-admissions-extractor/references/directus_collections_reference.md`) |
| 声称矩阵 | `T3_CLAIMS_MATRIX.md`(66 条声称 → 代码 → 测试) |
| 测试 | 98 tests OK:`tests/program_v3_rendering.test.mjs`(54)+ `tests/dom/program-card-v3.dom.test.tsx`(44);typecheck 0 error |
| 评审 | 实现 = 本会话;评审 = Codex —— 规则层四轮(T3-R3/R4/R5)+ 跨 ticket 两轮(T3-R6/R7) |

**本文档写于 T3 首次结项(四轮规则层评审后),§0 是后续追加的更新记录 ——
先看 §0 再看正文,正文的一些细节(测试数、fixture 数)已被 §0 的改动覆盖。**

---

## 0. 结项后续:T3-R6 / T3-R7(跨 ticket,2026-08-03,同日)

T3 结项之后、T4/T5 相继在同一目录开工并接入第一份**真实** Mode F 产出
(T1b 茱莉亚 Vocal Arts pilot 包)时,暴露了一个 T3 结项时四轮评审都没抓到的
缺陷,随后又对修复本身做了一轮复核。

### T3-R6 —— `cost_estimate_rmb.min/max` 单位错配(元 / 万)

**成因,也是这条最值得记住的地方:mock 数据与渲染代码共享了同一个错误假设。**
蓝图 §1.4 与 Mode F 真实产出的单位是**元**,但 T3 结项时的 mock fixture
(`min: 55, max: 66`)与 `costBlockLine()` 的显示逻辑(`` `¥${min}–${max}
万元人民币` ``)**都**把它当成「万」——两边彼此印证,没有任何东西不一致。

后果是:**四轮 Codex 评审 + 61 条声称矩阵全绿,没有一处发现它**。矩阵能钉住
"代码是否兑现了它声称的规则",但钉不住"声称本身是否对齐了真实契约"——
mock 与代码一起错,矩阵只会一起绿。**只有真实数据能拆穿这类共谋式错误**:
T5 接入 T1b 真实包(`min: 570000, max: 600000`)后,生产构建的 Web Card
渲染出「¥570000–600000 万元人民币」(= 57 亿元)。

修复:`costBlockLine()` 改为按元处理、除以 10,000 显示;
`lib/program-v3/json-ld.ts` 去掉重复的 `× 10_000`(它是跟着 T3 的错误假设
配套发明的第二次换算,同一个 bug 的两个症状)。

**新增的回归测试(`dom:U1`–`U5`,`tests/dom/program-card-v3.dom.test.tsx`)
刻意不经过 mock,直接读 `data/v3/real/juilliard-vocal-arts-pilot.json`
(经 `data/v3/real-programs.ts`,零改动)。这不是"顺便也测一下真实数据"——
这是精准钉住这次缺陷的成因本身:mock 和代码今后即使再次串通犯同一个错,
这组测试因为压根不用 mock,也会照样失败。** 这是比修复本身更有价值的部分:
修复解决了这一次,`dom:U1`–`U5` 防的是同一种错误再发生一次。

### T3-R7 —— 对 T3-R6 修复本身的复核:fix 自己重犯了它要防的错

T3-R6 的第一版修复是「除以 10,000 后 `toFixed(2)`」,注释写「只除法不舍入,
暴露契约违反」。但 `toFixed()` **本身就是舍入**:`9999` 和 `10001` 都会被它
显示成干净的「1 万」——契约违反被掩盖,不是被揭穿。函数声称和代码行为不符,
是这个项目反复出现的"声称与实现漂移"同一个家族,这次说谎的是函数自己的
注释。

裁决:**不追求"看起来好看",追求"违反契约就拒绝显示"**。
`costBlockLine()` 现在校验 `min`/`max` 是否为万的整数倍(`isWanAligned()`),
不满足则整块降级为原币种学费、`console.warn` 留痕,而不是想办法把一个
不该出现的数字格式化得体面。新增测试 `lib:F17`/`F18`/`F19` 直接用
9999/10001/12345 这三个真正暴露过旧 `toFixed()` 行为的输入。

副带澄清:Codex 两轮报告里 `toFixed(1)`/`toFixed(2)` 说法不一,核实后确认
实现从始至终只有 `toFixed(2)` 一个版本(现已随函数整体移除)——是报告本身
的观察漂移,不是代码漂移。

### 状态

**T3-R6、T3-R7 均已结项,不再开新轮次。** 与 T3b(路由迁移)的改动同文件
不同函数,已确认无冲突,全量回归绿。`T3_CLAIMS_MATRIX.md` / `T4_CLAIMS_MATRIX.md`
/ `T5_CLAIMS_MATRIX.md` / `T5_REVIEW_HANDOFF.md` 涉及费用渲染的行均已同步
更新为已解决状态。

---

## 1. 交付范围

**组件与 mock 数据,不接 Directus。** 数据管线在另一个仓库,本仓库不修改它。

| 文件 | 作用 |
|---|---|
| `data/v3/types.ts` | canonical + publishing 数据形状,对齐数据字典 §5–7 §13 |
| `data/v3/mock-programs.ts` | 9 个 fixture,日期全为固定字面量 |
| `lib/program-v3/format.ts` | 全部渲染规则(空值、截断、时间态、费用形态、条件归属) |
| `components/program/v3/*.tsx` | 卡片 7 块 + 详情页 7 模块,共 17 个组件 |
| `app/(explore)/v3-preview/**` | 预览路由(**非生产路由**,见 §5 T3b) |
| `tests/program_v3_rendering.test.mjs` | 纯规则,54 tests(含 T3-R6/R7 新增的 F15、F17–F19) |
| `tests/dom/program-card-v3.dom.test.tsx` | 需要文档才能断言的,44 tests(含 T3-R6 新增的 `U1`–`U5` 真实数据回归) |

**不在范围内(T3 结项时)**:JSON-LD / llms.txt / robots / sitemap(T4)、
Share Card / OG(T5)、聚合清单页(T6)、真实 Directus 接线。**T4/T5 均已在
同一目录落地,并接入了第一份真实 Mode F 包 —— 详见 §0。**

---

## 2. 四轮评审与裁决

| 轮次 | 发现 | 裁决 |
|---|---|---|
| 1(规则层) | 6 高危 4 中危 | **T3-R3**,11 条修复 + 1 条不修 |
| 2 | 1 高危 4 中危 | **T3-R4**,4 条修复 + 1 条不修 |
| 3 | 2 条 | **T3-R5**,2 条修复;人类宣布为最后一轮修复 |
| 4(确认性) | 无高危无中危,3 条低危 | 全部接受,**T3 结项** |

### 三轮修复里最值得记住的一条

**T3-R5.1 —— 判定方向比词表更重要。** 条件归属最初用「提到语言就并入英语行」的
关键词包含判定,Codex 用「作品集需包含英语作品」「作品集中包含 IELTS 曲目」
连破两次。继续加排除词是无限军备竞赛:排除「作品」还能来「面试用英语进行」。

R5 把问题反过来问:**默认独立成行,并入是窄例外**,需同时满足「命中语言词」
与「不命中任何其他材料词」,否决优先。两份词表的风险因此**不对称**——扩充否决表
只会把条件推向独立成行,永远安全;扩充语言表才会重开归错的口子。这条不对称性
写在 `conditionIsPurelyLanguage()` 的注释里,是留给后来人的操作规则。

代价:部分真语言条件会独立成行,读起来啰嗦。**啰嗦不是错误,归错才是。**

### 第四轮接受的三条低危

1. 词表外同义词(如「面谈用英语进行」)仍会误并——需构造触发,常见材料词已全部否决。
2. 「英语成绩证明材料需在申请时提交」被推向独立成行——保守规则的自然代价,
   原文完整呈现,无事实丢失。
3. 条件文本在「完整要求」与「特殊条件」两处重复——§2.2 这两个模块本就语义重叠。

---

## 3. 补测试时抓到的线上 bug

**三轮人工评审 + 三轮浏览器目检都没发现它。**

`deadlineState()` 原本用 `Math.ceil((deadline - now) / 86400000)` 算天数,
再用 `days < 0` 判是否已截止。问题:`Math.ceil(-0.5) === -0`,而 **`-0 < 0` 为 `false`**。
当天早些时候刚过期的截止日因此被判成未截止,渲染出「距截止 **-0** 天」
并保持开放态样式。

附带的第二个缺陷:按毫秒除法算天数会随一天内的时刻抖动——同一截止日
早上读「距截止 19 天」,晚上读「18 天」。

修法:两侧都下取到本地午夜,按**日历日**计算,`Math.round` 吸收 DST 的 ±1 小时。
钉死于 `lib:D1b`(三个时刻)与 `lib:D1c`(四个时刻读数恒等)。

**这条是补测试价值的证明。** 目检永远发生在某个具体时刻,而这个 bug 只在
「截止日 = 昨天」这个窗口里出现。对抗评审擅长找逻辑与边界缺陷,但评审者
构造输入靠的是对系统的想象;测试能把没人想到要看的那一格填上。

> 与 T2 的教训并列记住:T2 是**真实数据**抓到了六轮对抗评审没发现的缺陷
> (Directus 把 decimal 序列化成字符串)。T3 是**测试**抓到了三轮评审没发现的缺陷。
> 两者都不能相互替代,也都不能替代对方。

---

## 4. 关键裁决速查

改这些地方之前先读这一节,否则你会把已经裁决过的事重新做一遍。

| 裁决 | 内容 | 别改回去 |
|---|---|---|
| R3.1 | 「该块不渲染」指**容器本身不进 DOM**,空壳 `<div>` 不算 | 块组件自持间距,不要包 spacer `<div>` |
| R3.2 | 中文名缺失回退英文是**降级不是编造**(核心原则 6) | Codex 该条判断已明确不采纳 |
| R3.5 | §2.2 模块顺序冻结,**不得插入新模块** | 费用因此并进完整要求表;编辑观点与金标签因此不在详情页 |
| R3.6 | 卡片恰好七块,尾部无游离 CTA | 详情页入口 = 标题链接 + 折叠区内链接 |
| R3.7 | 截止角标**客户端计算,不进 SSR** | 事实(日期)已在 SSR DOM;角标进 SSR 会在静态构建时冻住,比不显示更糟 |
| R3.9 | 反 cloaking 不分文本重要性,**一律禁 CSS 截短** | 需要截断就用 JS 字符截断 |
| R4.1/R5.1 | 条件归属:默认独立成行,否决优先于命中 | 不要改回关键词包含判定 |
| R4.3/R5.2 | 费用形态**正向判定**:形态①② 需 tuition **与** living_cost 同时存在 | 缺任一半都不是「年总费用」 |
| R4.4 | 无详情页出口时**不截断**曲目 | 截断的前提是有地方读全文 |
| R5.3 | 条件文本两处重复**不修** | §2.2 两模块语义本就重叠 |
| R6 | `cost_estimate_rmb.min/max` 单位是**元**,不是万 | mock 与代码曾共享同一错误假设,只有真实数据能拆穿;回归靠 `dom:U1`–`U5` 直接读真实包,不经 mock |
| R7 | 契约违反(非万整数倍)→ **拒绝显示**,不是"格式化得好看" | `toFixed()` 会掩盖契约违反(9999/10001 都显示成「1 万」);改用 `isWanAligned()` 校验 + 降级 |

措辞裁决(非蓝图原文,由人类拍板):
`outdated_season` = 「上一申请季数据 · 官网核实 {cycle}」——过时信息前置,
避免「官网核实」打头被误读为新数据。

---

## 5. 未完事项

### T3b —— 生产路由迁移(**已完成**)

最终 URL 形态是蓝图的 `/schools/{school-slug}/{program-slug}`。T3 结项时
本仓库生产路由是 Directus 驱动的 `/schools/[schoolId]/programs/[programId]`,
与之冲突,组件当时挂在 `/v3-preview/...` 下。**T3b 会话已完成迁移**:
`programDetailHref()`(唯一出口,卡片/相关专业/JSON-LD `url`/sitemap `loc`
都走它)已改为返回两段式 slug URL,与 T3-R6/R7 改动的费用逻辑同文件不同
函数,已确认无冲突。

### T2b 新增一条 —— `conditional_notes` 字段级拆分

前端的关键词启发式(C2–C7)钉死的是判定方向,不是词表完备性。根治要靠 schema:
把 `application_requirements.conditional_notes` 拆成字段级归属
(做法同 T2b 已有的 `english_requirement_status` 补列),前端就不必再猜。

拆分落地后,`conditionIsPurelyLanguage()` 及其两份词表可整体删除。

### 其它待确认

- `FiveState` 的 `"Conditional"` 大小写按同级 PascalCase 约定写死,
  Directus dropdown 尚无此值,落库时需复核。
- **真实数据现状**:T1b 一个真实包(茱莉亚 Vocal Arts pilot)已进前端
  (`data/v3/real-programs.ts`,仍只挂在 `/v3-preview`)——不再是"零真实数据",
  但也仅此一个学校、一个 pilot。T3-R6 正是这一个真实包拆穿的缺陷,足以说明
  矩阵全绿**仍不能替代**继续接更多真实数据(T2 的教训在 T3 上完整重演过一次)。

---

## 6. 交给下一个 ticket 的话

T4(`ProgramJsonLd` / `CitationLine`)、T5(Share Card / OG / 微信分享)、
T3b(路由迁移)都已在同一目录落地,分别改动了 `FreshnessBar`、
`ProgramCardV3`、`programDetailHref()`。这类改动正是 `T3_CLAIMS_MATRIX.md`
的 `dom:` 组要防的:**T3 的结构性保证不能被后续 ticket 无声破坏**。

**T3-R6 是这条原则最有力的一次反证也是最有力的一次印证**:mock 与代码一起
错的时候,矩阵和 `dom:` 组一起绿,谁都拦不住;但一旦有一组测试(`dom:U1`–`U5`)
压根不吃 mock、直接吃真实数据,同样的错就无处可藏。**给下一个 ticket 的
具体建议:任何涉及数值单位、格式转换的渲染逻辑,配套测试里至少留一条直接
读真实数据源(而不是本 ticket 自己写的 mock/fixture)的回归**——这不是可选
的锦上添花,是防止"mock 与代码共享同一错误假设"这整类缺陷的唯一手段。

跑一遍:

```bash
npm run test:lib && npm run test:dom && npm run typecheck:incremental
```
